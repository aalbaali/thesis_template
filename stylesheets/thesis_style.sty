
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Packages

% Common commands/packages -------------->
\usepackage{decar-common}
% 'operating point'. TODO: Delete if not needed.
\newcommand{\op}{\mathrm{op}}

% helper bold symbols. TODO: Delete
\newcommand{\mbsch}[1]{\mbscheck{#1}}
\newcommand{\mbfch}[1]{\mbfcheck{#1}}

% Diagonal
\newcommand{\diag}[1]{\mathrm{diag}\left(#1\right)} % Norm
% <-------------- Common commands/packages

% Dynamics commands/packages -------------->
\usepackage{decar-dynamics}
% <-------------- Dynamics commands/packages

% Lie groups commands/packages -------------->
\usepackage{decar-lie}
% Exponential map wedge
\newcommand{\expmapw}[1]{\exp\left(#1^\wedge\right)}
% <-------------- Dynamics commands/packages

\usepackage{decar-post}

% Other commands/packages -------------->
% Use Rackham thesis style file
\usepackage{stylesheets/rac}
% To allow the reading of ADS journal references in the bibliography
\usepackage{stylesheets/aas_macros}

% Use in French abstract
\usepackage[french,english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{esint}
% Use various AMS packages
\usepackage{amsxtra}
\usepackage{amsfonts}
\usepackage{rotating}
\usepackage{color}
\usepackage{epsfig}
\usepackage{verbatim}
\usepackage{hhline}
% Allows you to specify the line spacing
\usepackage{setspace}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{adjustbox}
\usepackage{balance}
\usepackage{marginnote}
% landsape
\usepackage{lscape}
% \usepackage{pgfplots} % WARNING: Conflicts with \year definition
\usepackage{enumitem}
% <-------------- Dynamics commands/packages

% // Packages
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Options
% Spacing:
%   1.5 spacing:    \onehalfspacing, or
%   Double spacing: \doublespacing
\onehalfspacing

% Caption format
\DeclareCaptionFormat{citation}{%
   \ifx\captioncitation\relax\relax\else
     \captioncitation\par
   \fi
   #1#2#3\par}
\newcommand*\setcaptioncitation[1]{\def\captioncitation{\textit{Source:}~#1}}
\let\captioncitation\relax
\captionsetup{format=citation,justification=centering}

\MakeRobust{\underrightarrow}

\makeatletter
\def\BState{\State\hskip-\ALG@thistlm}
\makeatother

% Renew algorithm number to according to chapters.
\renewcommand{\thealgorithm}{\arabic{chapter}.\arabic{algorithm}}

% // Options
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Additional custom commands

% MATLAB in small caps
\newcommand{\matlab}[0]{{\textsc{matlab}\@}\xspace}
% Math definition (similar to \traingleq but slightly different formatting)
\long \def
  \isdef{
    \mathrel{
      \mathop{\smash{=}}
      \limits^{
        \hbox{\raisebox{.7ex}
        {\small$\scriptstyle\triangle$}
        }
      }
    }
  }

% Add text to the margin. TODO: Remove
\newcommand{\mymargin}[1]{\marginnote{{\footnotesize #1}}\index{#1}}

% Vectrix transposed. TODO: Remove
\newcommand{\vectrixT}[1]{{\ensuremath \underrightarrow{\boldsymbol{\mathcal{F}}}^{\ensuremath{\mathsf{T}}}_{#1}}}

% TODO: Remove
\newenvironment{packedItemize}{
\begin{itemize}
  \setlength{\itemsep}{1pt}
  \setlength{\parskip}{0pt}
  \setlength{\parsep}{0pt}
}{\end{itemize}}

% // Additional custom commands
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Various theorem environments. All of the following have the same numbering
% system as theorem.
% The commented out code is already defined in decar-common.sty package
\theoremstyle{definition}
% \newtheorem{theorem}{Theorem}
% \newtheorem{definition}[theorem]{Definition}
\newtheorem{notation}[theorem]{Notation}
\newtheorem{condition}[theorem]{Condition}
% \newtheorem{example}[theorem]{Example}
\newtheorem{introduction}[theorem]{Introduction}
\newtheorem{problem}[theorem]{Problem}
% \theoremstyle{remark}
% \newtheorem{remark}[theorem]{Remark}